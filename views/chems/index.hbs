{{#section 'stylesheets'}}
{{/section}}

<div class="">

  <div class="justify-content-center">

      <div class="text-center pb-3 gradient-background text-white">
        <h3 class="font-black">Princípios ativos</h3>
      </div>
      <div class="text-center p-3">
        <button onclick="location.href = '/chems/new'" class="btn font-book btn-outline-primary">Adicionar novo princípio ativo</button>
      </div>
  </div>

<!-- Chems list -->
<div style="overflow-x:auto;">
<table class="table table-light table-striped text-center">
  <thead>
    <tr>
      <th scope="col font-black">Nome</th>
      <th colspan="2" scope="col font-black">Opções</th>
    </tr>
  </thead>
  <tbody>
    {{#each chems}}
    <tr>
      <th scope="row" class="font-book">{{name}}</th>
      <th scope="row">
        <button onclick="location.href='/chems/{{id}}/edit'" class="btn btn-outline-warning font-book">Editar</button>
      </th>
      <th>
        <form action="/chems/{{id}}?_method=DELETE" method="POST">
          <button type="submit" class="btn btn-outline-danger font-book">Excluir</button>
        </form>
      </th>
    </tr>
    {{/each}}
  </tbody>
</table>
</div>
</div>
<!-- end chems list -->

{{#section 'scripts'}}
{{/section}}
